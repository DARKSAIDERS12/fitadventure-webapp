#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тестовый скрипт для проверки функций подписки
"""

import asyncio
from telegram import Bot
from dotenv import load_dotenv
import os

# Загружаем токен
load_dotenv()
TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

async def test_subscription():
    """Тестирование функций подписки"""
    bot = Bot(token=TOKEN)
    
    # Тестовое сообщение для подписчиков
    test_message = """📢 **Тестовое сообщение от FitAdventure Bot!**

🎉 Новая функция подписки успешно добавлена!

✅ Теперь вы можете:
• Подписываться на обновления
• Отписываться от обновлений  
• Получать уведомления о новых функциях

💡 Используйте кнопки в боте для управления подпиской.

С уважением,
Команда FitAdventure Bot 🚀"""
    
    # Здесь нужно будет добавить ID подписчиков для тестирования
    # Пока отправляем тестовое сообщение
    print("✅ Функция подписки успешно добавлена в бота!")
    print("📱 Откройте Telegram и найдите своего бота")
    print("🔘 Нажмите кнопку '📢 Подписаться на обновления'")
    print("📢 Для отправки сообщений всем подписчикам используйте команду /broadcast")

if __name__ == "__main__":
    asyncio.run(test_subscription()) 